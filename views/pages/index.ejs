<h3>-------------------------------------------------------------------------------------------------:----  Item List   ----:------------------------------------------------------------------------------------------------</h3>

<div class="row" style="margin-left:30px;margin-right:30px;">
<div class="">
 <div class="col s12">
  <a href="/event/add" class="btn btn-success btn-lg">Add a new item</a>
  </div>
  </div>
  <br>

  <div class="=">
   <div class="col s12">
   <% if (items.length > 0) { %>
  <table class="table">
    <thead>
    <tr>
    <th style="width:1%;text-align:center">s.no.</th>
    <th style="width:20%">name</th>
    <th style="width:30%">description</th>
    <th style="width:30%">url</th>
    <th style="width:20%">actions</th>
    <th solsapn="2" style="width:20%;text-align:center">Autogenerated id</th>
    </tr>
    </thead>
    <tbody>
    <% items.forEach(function(item, index){ %>
    <tr>
    <td style="width:1%;text-align:center"><%= index + 1 %></td>
    <th style="width:20%"><%= item.name %></th>
    <th style="width:30%"><%= item.description %></th>
    <td style="width:20%"><%= item.url %></td>
    <td solspan="2" style="width:20%,text-align:center">
    <a href="/event/edit/<%=item.id %>" class="btn btn-success">Update</a>
    <a href="/event/delete/<%= item.id %>" class="btn btn-danger">Delete</a>
    </td>
    <td style="width:10%"><%= item.id %></td>
    </tr>
    <% }); %>
    </tbody>
    </table>
    <% }else{ %>
    <p><strong>There are no records available</strong></p>
    <% } %>
    </div>
    </div>